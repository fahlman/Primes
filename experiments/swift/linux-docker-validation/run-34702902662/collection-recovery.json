{
  "scope": "Evidence download recovery only; no Docker, Swift build, verification or timing was repeated.",
  "initial_attempt": "amd64/collection.json",
  "second_attempt": "amd64-job-log-retry1/collection.json",
  "successful_attempt": "amd64-raw-log-retry2/collection.json",
  "observed_probe": {
    "command": [
      "gh",
      "api",
      "--method",
      "GET",
      "repos/fahlman/Primes/actions/jobs/103577690048/logs"
    ],
    "exit_code": 1,
    "stderr": "the response contains terminal escape sequences; pass --allow-escape-sequences to output it anyway\n"
  },
  "resolution": "Capture original API response bytes into files using --allow-escape-sequences. Raw logs are not emitted to a terminal. Full-suite artifact checks remain unchanged.",
  "note": "An earlier job-log probe while the job was running returned HTTP404. The failed collector records did not capture stderr, so their exact error bodies are unavailable; no cause is retroactively added to them. The separate direct probe establishes the CLI escape-sequence condition before the successful collection."
}
